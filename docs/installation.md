# Installation

## Using npm

The recommended way to install LlamaNeuro is via npm:

```bash
npm install llamaneuro
```

## From Source

You can also install LlamaNeuro from source:

```bash
git clone https://github.com/llamasearchai/llamaneuro.git
cd llamaneuro
npm install
```

## Dependencies

LlamaNeuro dependencies are managed by npm.
